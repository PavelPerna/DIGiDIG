# =============================================================================# Database Configuration

# DIGiDIG Email System - Environment Configuration TemplateDB_USER=postgres

# =============================================================================DB_PASS=securepassword

# This is a template file. Copy it to .env and update values as needed:DB_NAME=strategos

#   cp .env.example .env

# WARNING: Never commit .env file to version control!# Admin Credentials

# =============================================================================ADMIN_EMAIL=admin@example.com

ADMIN_PASSWORD=admin

# -----------------------------------------------------------------------------

# DATABASE CONFIGURATION (PostgreSQL)# JWT Configuration

# -----------------------------------------------------------------------------JWT_SECRET="your-secret-key-here"

# PostgreSQL connection settings used by Identity service

DB_HOST=postgres# JIRA Integration (optional)

DB_USER=postgresJIRA_BASE_URL="https://your-instance.atlassian.net"

DB_PASS=securepasswordJIRA_API_TOKEN="your-jira-api-token"

DB_NAME=strategosJIRA_USER_EMAIL="your-email@example.com"



# -----------------------------------------------------------------------------# OpenAI Integration (optional)

# MONGODB CONFIGURATIONOPENAI_API_TOKEN="your-openai-api-token"

# -----------------------------------------------------------------------------OPENAI_API_BASE_URL="https://api.openai.com/v1"

# MongoDB connection settings used by Storage service

MONGO_URI=mongodb://mongo:27017# GitHub Integration (optional)

STORAGE_MAX_DOC_SIZE=16777216GITHUB_API_TOKEN="your-github-token"


# -----------------------------------------------------------------------------
# IDENTITY SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# Authentication and user management service settings
IDENTITY_HOSTNAME=0.0.0.0
IDENTITY_PORT=8001
IDENTITY_TIMEOUT=30

# JWT Token Configuration - CHANGE IN PRODUCTION!
JWT_SECRET=b8_XYZ123abc456DEF789ghiJKL0mnoPQ
TOKEN_EXPIRY=3600
REFRESH_TOKEN_EXPIRY=1209600

# Default Admin Account (auto-created on first run) - CHANGE IMMEDIATELY!
# Username and domain are extracted from ADMIN_EMAIL (split by @)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin

# -----------------------------------------------------------------------------
# SMTP SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# SMTP server and email sending service settings
# Note: SMTP authentication is handled via Identity service (no separate credentials needed)
SMTP_HOSTNAME=0.0.0.0
SMTP_PORT=8000
SMTP_SERVER_PORT=2525

# SMTP Performance Settings
SMTP_MAX_WORKERS=4
SMTP_POOL_SIZE=10
SMTP_TIMEOUT=30
SMTP_RETRY_ATTEMPTS=3
SMTP_RETRY_DELAY=2

# SMTP Limits
SMTP_MAX_MESSAGE_SIZE=10485760
SMTP_QUEUE_SIZE=100

# -----------------------------------------------------------------------------
# IMAP SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# IMAP server and email retrieval service settings
IMAP_HOSTNAME=0.0.0.0
IMAP_PORT=8003

# IMAP Performance Settings
IMAP_MAX_WORKERS=4
IMAP_POOL_SIZE=10
IMAP_TIMEOUT=30
IMAP_MAX_CONNECTIONS=50
IMAP_IDLE_TIMEOUT=300

# -----------------------------------------------------------------------------
# STORAGE SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# Email storage and persistence service settings
STORAGE_HOSTNAME=0.0.0.0
STORAGE_PORT=8002
STORAGE_TIMEOUT=30

# -----------------------------------------------------------------------------
# ADMIN SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# Admin web interface settings
ADMIN_PORT=8005

# -----------------------------------------------------------------------------
# CLIENT SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# Client web interface settings
CLIENT_PORT=8004

# -----------------------------------------------------------------------------
# EXTERNAL INTEGRATIONS (Optional)
# -----------------------------------------------------------------------------

# Jira Integration
JIRA_BASE_URL=https://your-org.atlassian.net
JIRA_API_TOKEN=your_jira_api_token_here
JIRA_USER_EMAIL=your.email@example.com

# OpenAI Integration
OPENAI_API_TOKEN=your_openai_api_key_here
OPENAI_API_BASE_URL=https://api.openai.com/v1

# GitHub Integration
GITHUB_API_TOKEN=your_github_token_here

# -----------------------------------------------------------------------------
# DEVELOPMENT & TESTING
# -----------------------------------------------------------------------------
# These variables are used during development and testing

# Test Environment
BASE_URL=http://localhost:8001
SKIP_COMPOSE=0

# MCP Server (Model Context Protocol)
LOG_LEVEL=DEBUG
PORT=3001

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Change all default passwords before deploying to production!
# 2. Generate strong JWT_SECRET: openssl rand -hex 32
# 3. Never commit .env file to version control
# 4. Use different credentials for each environment (dev/staging/prod)
# 5. Rotate API tokens and secrets regularly
# 6. Consider using secrets management service (Vault, AWS Secrets Manager)
# =============================================================================
