# DIGiDIG Production Environment Configuration
# Copy this file to .env.prod and customize with secure values
# ⚠️  NEVER commit this file to version control!

# GitHub Container Registry
GITHUB_REPOSITORY_OWNER=your-github-username
IMAGE_TAG=v1.0.0

# Database Configuration (⚠️ Use strong passwords!)
DB_NAME=digidig_production
DB_USER=prod_user
DB_PASS=CHANGE_ME_PRODUCTION_DB_PASSWORD_VERY_SECURE

# MongoDB Configuration (⚠️ Use strong passwords!)
MONGO_USER=prod_mongo
MONGO_PASS=CHANGE_ME_PRODUCTION_MONGO_PASSWORD_VERY_SECURE

# JWT Configuration (⚠️ Use cryptographically secure secret!)
JWT_SECRET=CHANGE_ME_PRODUCTION_JWT_SECRET_256_BITS_MINIMUM

# Admin Account (⚠️ Use strong password!)
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=CHANGE_ME_PRODUCTION_ADMIN_PASSWORD_VERY_SECURE

# SMTP Configuration (⚠️ Production SMTP settings required!)
SMTP_HOST=smtp.your-domain.com
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=CHANGE_ME_PRODUCTION_SMTP_PASSWORD
SMTP_PORT=587
SMTP_USE_TLS=true

# Additional Configuration
DIGIDIG_ENV=production

# Security Notes:
# 1. Use passwords with at least 20 characters
# 2. Include uppercase, lowercase, numbers, and symbols
# 3. Use different passwords for each service
# 4. Rotate passwords regularly
# 5. Never share this file or commit it to git
# 6. Consider using environment-specific secret management