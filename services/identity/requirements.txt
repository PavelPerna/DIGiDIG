# Service-specific dependencies (common deps in base image)
# Base image provides: fastapi, uvicorn, aiohttp, httpx, pydantic, pyyaml, jinja2, requests, python-multipart


asyncpg==0.29.0
pyjwt==2.9.0
cryptography==42.0.5
