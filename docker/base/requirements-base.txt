# Common dependencies for all DIGiDIG services
# This file is used to build the base Docker image

# Core web framework (used by ALL services)
fastapi==0.115.0
uvicorn==0.30.6

# HTTP clients (used by ALL services)
aiohttp==3.10.5
httpx==0.27.0

# Data validation (used by 10/11 services)
pydantic==2.9.2

# Configuration & templating (used by 10/11 services)
PyYAML>=6.0
jinja2==3.1.4

# HTTP utilities (used by 10/11 services)
requests==2.31.0

# Form handling (used by 6/11 services)
python-multipart==0.0.12
